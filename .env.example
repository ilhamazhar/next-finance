# Backend API base (server-side only — never NEXT_PUBLIC_)
BACKEND_API_URL=http://localhost:6001

# Cookie name + secret for httpOnly refresh cookie
COOKIE_SECRET=replace-with-long-random-string-min-32-chars
COOKIE_NAME=azhar_rt

# Set to 1 in production (https only)
COOKIE_SECURE=0
